#Obandit

##CLI Interface

Obandit-`r Sys.getenv('obanditversion')` ships with a helper binary.

For now this binary only runs bandit strategies on csv files:


```{bash}
obandit --help
```

```{bash}
obandit csv --help
```
